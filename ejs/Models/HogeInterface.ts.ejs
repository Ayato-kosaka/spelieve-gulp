import { GeoPoint, Timestamp, DocumentReference } from "firebase/firestore/dist/firestore";

/**
 * Export interface of <%- table.t_id %><%- table.t_name %> collection.
 */
export interface <%- table.t_id %><%- table.t_name %>Interface {
<% columns.forEach((column) => { -%>
    <%- column.c_name %>: <%- dataType.find(x => x.FirestoreType === column.c_datatype).DBType %>;
<% }); -%>
};