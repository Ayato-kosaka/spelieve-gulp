import { <%- name %>Interface } from './<%- name %>Interface';

/**
 * Export Object of column names of <%- name %> collection.
 */
export const <%- name %>Cols: { [P in keyof <%- name %>Interface]: string } = {
<% columns.forEach((column) => { -%>
    <%- column.c_name%>: '<%- column.c_name%>',
<% }); -%>
}